import { BlurToggleDirective } from './blur-toggle.directive';

describe('BlurToggleDirective', () => {
  it('should create an instance', () => {
    const directive = new BlurToggleDirective();
    expect(directive).toBeTruthy();
  });
});
